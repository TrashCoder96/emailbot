main:
  host: localhost
  port: 20090
bots:
 sql_dialect: org.hibernate.dialect.PostgreSQL9Dialect
server:
  host: localhost
  port: 20091
  name: SKYPE
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/emailbot?createDatabaseIfNotExist=true
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver